{
  "dependencies": {
    "express": "^4.13.4",
    "react": "^15.1.0",
    "react-dom": "^15.1.0",
    "socket.io": "^1.4.6",
    "socket.io-client": "~1.4.6",
    "mathjs": "~3.2.1"
  },
  "scripts": {
    "run": "tsc && browserify dist/client.js -o public/bundle.js && DEBUG=socket.io:socket node dist/server.js -w src/",
    "tsc": "tsc",
    "setup": "npm install && tsd install"
  },
  "devDependencies": {
    "browserify": "^13.0.1",
    "tsd": "^0.6.5",
    "typescript": "^1.8.10"
  }
}
