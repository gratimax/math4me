{
  "dependencies": {
    "socket.io": "^1.4.6"
  },
  "scripts": {
    "build": "tsc",
    "setup": "npm install && typings install"
  },
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.0.4"
  }
}
