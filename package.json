{
  "dependencies": {
    "socket.io": "^1.4.6"
  },
  "scripts": {
    "run": "tsc && node dist/index.js",
    "setup": "npm install && typings install"
  },
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.0.4"
  }
}
